return {
	"akinsho/bufferline.nvim",
	dependencies = { "nvim-tree/nvim-web-devicons" },
	version = "*",
	opts = {
		options = {
			mode = "tabs",
			-- separator_style = "slant", -- (this is commented out because I want the bufferline to be straight similar to vscode, if you want to change it feel free)
		},
	},
}
